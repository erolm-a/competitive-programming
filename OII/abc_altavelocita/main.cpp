#include <cstdio>
#include <algorithm>
#include <utility>

#define MAX_F 1000

pair<int, int> binari[MAX_F];
int N, B;

using namespace std;

int main(int argc, char *argv[])
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}
