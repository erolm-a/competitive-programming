#include <cstdio>
#include <cstring>
#include <vector>

using namespace std;



int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    int H, W, X;
    scanf(" %d %d %d", &H, &W, &X);

    // pattern:
    // x00
    // 00x
    // xx0
    //
    // affinché non si ripeta ogni due righe si scrolla di uno
}
